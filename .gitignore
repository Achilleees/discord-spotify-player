# Build artifacts
/target/
*.zip
releases/

# Environment and secrets
.env
*.pem
*.key

# Spotify credentials cache
credentials.json
.spotify_cache/

# Per-user OAuth tokens (plaintext until the SQLite migration)
.user_creds/

# IDE
.idea/
.claude/
*.swp
*.swo

# Local tooling (config.toml is tracked: it carries the cmake build fix)
.cargo/*
!.cargo/config.toml

# OS
.DS_Store
Thumbs.db
