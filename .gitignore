# Build artifacts
/target/
*.zip
releases/

# Machine-specific build helper (hardcodes a local MSVC path)
build.bat

# Environment and secrets
.env
*.pem
*.key

# Spotify credentials cache
credentials.json
.spotify_cache/

# Per-user OAuth tokens: legacy JSON dir + the SQLite credential store
# (glob covers -shm/-wal/-journal sidecars)
.user_creds/
.user_creds.migrated/
spotibot.db*

# IDE
.idea/
.claude/
*.swp
*.swo

# Local tooling (config.toml is tracked: it carries the cmake build fix)
.cargo/*
!.cargo/config.toml

# OS
.DS_Store
Thumbs.db
target-alt/
